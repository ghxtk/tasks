#include <iostream>
#include <string>
using namespace std;

int main() {
	string s;
	getline(cin, s);
	char a; cin >> a;
	for (int i = 0; i < s.length(); i++) {
		if (s[i] == a) {
			cout << s[i] << s[i];
		}
		else
			cout << s[i];
	}
  return 0;
}
