#include <iostream>
using namespace std;

int main() {
	float  x, b = -1;
	int n;
	cin >> n;
	cin >> x;
	float s = 0;
	for (int i = 0; i <= n; i++) {
		b *= -(x / ((i + 1) * (i + 2)));
		s += b;
	}
	cout << s;
	return 0;
}
