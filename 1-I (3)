#include <iostream>
#include <cmath>
using namespace std;

int main() {
	setlocale(LC_ALL, "rus");
	int x;
	cout << "Введите Х ";
	cin >> x;
	cout <<"Значение выражения равно " << sqrt(pow(x, 4) + sqrt(fabs(x + 1)));
	return 0;
}
