#include <iostream>
using namespace std;

int main() {
	double x, y;
	char w;
	cin >> x >> y;
	cin >> w;
	switch (w) {
	case '+': 
		cout << x + y;
		break;
	case '-': 
		cout << x - y;
		break;
	case '*': 
		cout << x * y;
		break;
	case '/': 
		cout << x / y;
		break;
	}
	return 0;
}
