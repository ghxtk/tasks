#include <iostream>
using namespace std;

int main() {
	int a[10];
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	for (int j = 0; j < n; j++) {
		if (j % 2 == 0) {
			cout << a[j] << " ";
		}
	}
	return 0;
}
