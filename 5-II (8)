#include <iostream>
using namespace std;

int main() {
	double  x, b = -1;
	int n;
	cin >> n;
	cin >> x;
	float s = 0;
	for (int i = 1; i <= n; i++) {
		b *= - x/((2*i-1)*(2*i));
		s += b;
	}
	cout << s;
	return 0;
}
